# Accelerated Optimized Instances

- Use case: Applications that need good local Input / Output (IO), great for workloads that require high, sequential read / write access to very large datasets on local storage.
- Example: To Be Added (TBA).